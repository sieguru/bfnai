# Server
PORT=3009
NODE_ENV=development

# MySQL Database
# For Railway: Use the DATABASE_URL or individual vars from Railway MySQL addon
# For PlanetScale: Use the connection string
DB_HOST=localhost
DB_PORT=3306
DB_NAME=legal_rag
DB_USER=root
DB_PASSWORD=

# Qdrant Vector Database (Cloud recommended for deployment)
QDRANT_URL=https://your-cluster.cloud.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key
QDRANT_COLLECTION=legal_chunks

# Embeddings
EMBEDDING_PROVIDER=voyage
EMBEDDING_MODEL=voyage-law-2
VOYAGE_API_KEY=your-voyage-key
# OPENAI_API_KEY=your-openai-key  # If using OpenAI instead

# Anthropic (for AI Agent)
ANTHROPIC_API_KEY=your-anthropic-key
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Processing defaults
DEFAULT_CHUNK_SIZE=500
DEFAULT_CHUNK_OVERLAP=1

# CORS (production only - comma-separated origins)
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
